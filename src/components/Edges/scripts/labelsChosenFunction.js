((values, id, selected, hovering) => {
    if (selected) {
        values.color = "red";
        values.size = 18;
    }
    //values.face
    //values.mod - bold, italic, etc.
    //values.vadjust
    //values.strokeWidth
    //values.strokeColor
})